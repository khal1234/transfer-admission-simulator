# 프로젝트 작업 지침

## 사이트 업데이트 알림 동기화

- 이 프로젝트에서 코드 수정, 버그 수정, 기능 추가, 데이터·환산식 변경 또는 사용자 체감 성능 개선을 하면 작업을 끝내기 전에 반드시 `web/src/components/updateNoticeContent.ts`의 사이트 업데이트 알림도 함께 갱신한다.
- 업데이트 알림의 날짜는 작업 완료일(Asia/Seoul)로 바꾸고, 사용자가 이해할 수 있는 짧은 한국어 문장으로 최신 변경 사항을 목록 맨 위에 추가한다.
- 기존의 중요한 업데이트 이력은 이유 없이 삭제하지 않는다. 항목이 너무 많아지면 최신 변경을 우선하고, 오래된 항목만 묶어서 요약한다.
- 커밋·푸시 요청을 받았을 때 사이트 동작이 바뀌었는데 업데이트 알림 변경이 빠져 있으면, 먼저 알림을 보완하고 테스트한 뒤 함께 커밋한다.

### 기계 검사 (2026-08-12 추가)

이 규칙은 문서에만 있어서 지켜지지 않았다 — 2026-08-12 학과명 정규화 등 3건이
알림 없이 커밋·푸시됐고 아무것도 걸리지 않았다. 그래서 검사를 붙였다.

```bash
python tools/check_update_notice.py            # 커밋된 것 기준
python tools/check_update_notice.py --staged   # 커밋 직전
```

`web/src/**`·`web/public/**`·`results/**` 를 건드린 마지막 커밋 날짜가
`UPDATE_NOTICE_DATE.iso` 보다 최신이면 exit 1 이다(테스트 파일과 알림 파일 자신은
제외). **`tools/deploy.ps1` 이 빌드 전에 이 검사를 부르고, 실패하면 배포를
중단한다.**

`accessibility.test.tsx` 는 알림의 **모양만** 본다(iso 형식, label 과 iso 일치,
항목 비어 있지 않음, 중복 없음). 예전에는 날짜와 문구를 통째로 못 박아 뒀는데,
그러면 **잊었을 때 통과하고 갱신했을 때 깨진다** — 방향이 반대라 바꿨다.

## Git 반영 원칙

- 이 프로젝트의 커밋·푸시는 사용자가 다른 브랜치를 명시적으로 지정하지 않는 한 항상 `main` 브랜치에 직접 반영하고 `origin/main`으로 푸시한다.
- 기본 작업 흐름에서 기능 브랜치나 PR을 새로 만들지 않는다. 다른 브랜치에서 작업한 변경이 있으면 검증 후 `main`에 안전하게 반영한 다음 푸시한다.
- 푸시가 끝나면 로컬 `main`과 원격 `origin/main`에 최종 커밋이 반영됐는지 확인한다.

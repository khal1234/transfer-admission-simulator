# Worklog

## Current Status

- npm 의존성 감사 결과 알려진 취약점은 0건이다.
- 프론트엔드 앱은 `web/`에 있고, 결과 데이터 산출물은 `results/`와 `web/src/data/`에 분리돼 있다.
- `results/`와 `web/src/data/`를 건드리지 않고 계획한 프론트엔드 안전성·렌더링 최적화를 완료했다.
- 합격 평균 비교는 최신 유효 연도로 fallback하며, 비교 불가 상태를 성공으로 표시하지 않도록 보강했다.
- 점수 입력과 localStorage 복원값은 검증하며 저장 실패 시 세션 한정 상태를 사용자에게 알린다.
- 대학·연도별 환산식과 역산 기울기는 `formulaRegistry.ts`를 SSOT로 사용한다.
- 환산식은 계산 정확도와 연도별 근거 상태를 분리해 안내하며, 다른 연도 공식을 가정한 경우를 명시한다.
- 공식 지원 연도는 2024~2026으로 제한하며 범위 밖 연도는 계산 불가로 처리한다.
- 표준·예외 JSON은 사용 전에 shape와 중복 키를 검증하고, 예외 전형 이력은 미선발과 구분해 표시한다.
- 의미상 이상 데이터는 원본을 수정하지 않고 런타임에서 제외·비공개 처리하며 보정 건수를 사용자에게 알린다.
- UI는 독립 렌더 경계로 분리했고 최상위 렌더 실패에는 복구 화면을 제공한다.
- 모바일 320~390px 화면에서 헤더 액션, 터치 영역, 안전영역과 지망 바로가기를 실화면 기준으로 보정했다.

## Recent Changes

- GitHub 저장소 첫 화면용 루트 README에 기능, 실행·검증 방법, 데이터 처리 원칙과 제한사항을 정리했다.
- 비교 가능 레코드 선택과 비교 불가 안내를 추가해 잘못된 성공 표시를 차단했다.
- 점수 입력·GPA 스케일 변환·저장된 지망 목록 검증을 각각 공용 유틸로 분리했다.
- 9개 대학·3개 연도의 기존 환산값을 특성 테스트로 고정하고 공식을 레지스트리로 통합했다.
- Recharts lazy 청크와 UI 렌더 경계를 분리하고 런타임 진행률 외 정적 inline style을 CSS로 이전했다.
- 지망 카드 상태 스타일을 CSS로 옮기고 저장소 접근을 실패 여부를 반환하는 단일 경계로 통합했다.
- 최상위·차트 오류 경계와 저장 실패 안내를 추가했으며 102건 테스트, TypeScript strict, lint, build를 통과했다.
- 취약한 간접 의존성을 `postcss 8.5.23`, `nanoid 3.3.16`으로 갱신해 npm audit 0건을 확인했다.
- 하드코딩된 데이터 건수를 실제 레코드·모집단위 수로 대체하고, 환산 차트에 추정·근사식 안내를 추가했다.
- 외부 CDN 폰트 요청을 제거하고 한국어 문서 정보와 검색 입력 접근성을 보강했다.
- 공식의 대표 중간값·구간 하한·입력 클램프 테스트를 추가하고 예외 이력 JSON을 지연 로딩 청크로 분리했다.
- GPA 필수 공식의 부분 점수 비교를 차단하고 숫자 학과명·범위 밖 점수·비공개 0값을 안전하게 정규화했다.
- 상단에 AI 생성·공식 자료 2차 검증·최종 판단 책임 안내문을 추가하고 모바일 배치를 확인했다.
- 모든 입결 추이 그래프는 데이터 지점을 곡선 보간 없이 직선으로 연결한다.
- 소형 모바일 헤더는 상태와 액션을 2단 배치하고, 장바구니가 보일 때는 고정 지망 버튼을 숨겨 콘텐츠 가림을 줄였다.

## Next Tasks

- 추가 코드 작업 시 `results/`는 계속 제외하고, `web/src/data/` 수정은 사용자가 명시한 경우에만 진행한다.
- 강원대학교 2026 환산식 변경은 사용자 요청에 따라 보류했으며 현재 추정식을 유지한다.
- 메인 번들은 약 960KB(gzip 약 126KB)이며, 예외 이력은 별도 약 127KB(gzip 약 7.6KB) 청크로 분리했다.
- 비동기 데이터 로딩이 제품 요구사항이 되면 초기 로딩 UI와 함께 데이터 청크 분리를 다시 검토한다.
- 모바일 후속 작업에서는 실제 iOS Safari·Android Chrome의 키보드 입력과 가로 모드를 기기에서 확인한다.

## Update Rules

- 작업이 끝난 뒤 이 파일에는 긴 대화 로그 대신 결과 중심 요약만 남긴다.
- 한 번 업데이트할 때는 현재 상태, 최근 변경, 다음 작업 위주로 3개에서 7개 정도의 짧은 항목만 유지한다.
- 이미 끝난 세부 이력보다 다음 대화에 필요한 맥락과 주의사항이 우선이다.

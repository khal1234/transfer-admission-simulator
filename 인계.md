# 세션 인계

> 마지막 갱신 2026-08-16. 다음 세션이 **이 파일부터** 읽으면 되도록 열린 것만 남긴다.
> 끝난 일의 경위는 커밋 메시지에 있다 — 여기 옮겨 적지 않는다.

## ★ 이 리포는 PUBLIC 이다 — 푸시 판정이 다른 리포와 다르다

공용 `규칙/AGENTS.md` 실행 규율 14 는 *"커밋·푸시 여부는 묻지 말고 판단해서 제안한다"* 이고,
그 근거로 **«비공개 원격에서는 「게시라 못 되돌린다」가 성립하지 않는다»** 를 든다.

**여기는 그 예외가 안 걸린다.** `khal1234/transfer-admission-simulator` 는 **공개 저장소**다
(실측 2026-08-16 `gh repo view --json isPrivate` → `false`). 그러므로 규율 7 의 원래 근거가
그대로 살아 있고, **푸시는 사용자 승인을 받는다.** 로컬 커밋은 자유롭게 한다(되돌릴 수 있다).

같은 이유로 **커밋 메시지도 공개본**이고, 올라갈 파일은 `scan_private` 을 먼저 돌린다.

## 지금 상태 (2026-08-16)

- `main` 이 `origin/main` 보다 **앞서 있다 — 미푸시.** 아래 「열린 것」 1번.
- 저점 점검 **✘ 0건 · 면제 1건**(`자기 사본으로 도나`, 사유는 `저점-면제.txt`).
- **세션 담당이 바뀌었다.** 이 리포는 `knu-bot` 폴더에서 도는 세션이 함께 관리한다
  (2026-08-15 사용자 지시). 그 세션이 다루는 범위는 `잡탕/HANDOFF.md` 에 표로 있다 —
  `잡탕`·`knu-bot`·`knu-rl-2026`·`편입` 이고 **전공정리프로젝트는 제외**다.

## 열린 것

1. **미푸시 커밋 — 사용자 승인 대기.** 공개 저장소라 위 절에 따라 자동으로 밀지 않는다.
   올라갈 파일은 `scan_private` 0건을 확인해 두었다(2026-08-16).
2. **공용 시스템 변경 20건** — 세션 시작 훅이 알리고 있다. `Documents/Claude/변경일지.md` 의
   해당 줄만 읽고 편입이 가져갈지 항목별로 판정한다. 가져왔으면
   `python .claude/hooks/shared_sync_check.py --accept <경로…>` 로 표시한다.
   ★ **통째로 accept 하지 않는다** — 아직 판정 안 한 파일까지 «확인함» 이 되어 다음 회차에 조용히 덮인다.
3. **지적 원장** — 지적이 모이는 자리가 아직 없다. 공용 도입 대장의 `편입,기록/feedback-ledger.md`
   행에 «필요 없다가 아니라 아직 안 세웠다» 로 적어 두었다.

4. **[제안] `design-qa.md` 5~7행의 협업자 개인 기계 경로** — 급하지 않다. 판정만 받으면 된다.

   ```
   5: /var/folders/hw/…/codex-clipboard-….png     (macOS 임시 경로)
   6: /Users/choikang/Documents/…/13-compare-portal-final.png
   7: /Users/choikang/Documents/…/compare-source-vs-final.png
   ```

   공용 판정선 ⑵기계 경로에 해당하고 **이 리포는 공개**라 그대로 노출돼 있다.
   세 줄 다 UX 감사 스크린샷의 경로 나열이라 **지워도 문서 내용이 상하지 않는다.**

   **손대지 않은 이유:** 작성자가 협업자(`choikang6367`)이고 **이미 원격에 올라가 있다**
   (`aaf0aa1`, 2026-08-09). 이 문서 아래 「남의 작업물을 고칠 때는 작성자를 먼저 확인한다」와
   2026-07-31 철회 전례가 그대로 걸린다.

   **무게 판단:** `choikang6367` 이라는 계정명이 이미 공개라 홈 경로의 `choikang` 이
   추가로 알려주는 것은 거의 없다. **급한 사안이 아니다.** 다만 지우는 비용이 세 줄이고
   앞으로 같은 것이 안 쌓인다.

   | 안 | 무엇 | 비고 |
   |---|---|---|
   | 가 | 협업자에게 말하고 그쪽이 지운다 | 원칙대로. 그동안은 남아 있다 |
   | 나 | 사용자 판단으로 바로 지운다 | 빠르다. 남의 파일을 고치는 것 |
   | 다 | 그대로 두고 예외 대장에 사유를 적어 면제 | 노출 무게가 작다고 보면 |

   **판정 전까지 `scan_private` 은 3건으로 남는다**(design-qa 2줄 + 예외 대장의 자기참조 1줄).
   그 3건이 무엇인지 알고 있으므로 **다른 작업의 게이트로는 이 숫자를 기준선으로 쓴다** —
   3건보다 늘면 새로 생긴 것이다.

## 닫은 것 (2026-08-16)

- **`cost_brief.py` 판정 — 도입.** 공용 `훅/cost_brief.py` 와 **바이트 동일**함을 확인했고
  (`diff` 로 CRLF 제외 비교), 단독 실행 `exit 0`. `SessionStart` 에서 세션 비용 추세를 한 줄로
  알리는 훅이고 **차단 판정을 하지 않는다** — 협업자 기계에서 돌아도 작업을 막지 않는다.
  같은 훅을 `knu-bot` 에도 같은 근거로 넣었다.
- **`.claude/launch.json` — 도입.** `web/` 개발 서버 실행 설정이고 상대 경로·포트만 담아
  기계에 종속되지 않는다(`npm --prefix web run dev`, 5173). 협업자도 그대로 쓸 수 있다.
- **개인정보 게이트를 세웠다 — `개인정보-예외.txt` 신설.** 이 리포는 공개인데
  `scan_private` 을 돌린 적이 없었다. 처음 돌리니 **11,534건**이 나왔고 대부분이 대학 이름,
  즉 **이 프로젝트가 다루는 도메인 데이터**였다(자체 대장이 없어 공용 대장을 쓰고 있었다).
  판정 기준을 하나로 두고 갈랐다 — **그것이 다루는 「대상」인가, 만든 사람에 관한 정보인가.**
  → **11,534 → 3건.** 남은 3건은 위 「열린 것」 4번이다.
  ★ 이 대장은 **폴더 경로로 훑어야 먹는다.** 파일 하나를 지정하면 `find_ledger` 가
  그 파일의 부모까지만 보고 공용 대장을 쓴다(실측). 파일 단위로 낸 「0건」은 이 리포 기준이 아니다.

## 주의 — 이 리포에만 있는 것

- **협업자가 있다.** 프런트·UI 는 choikang6367 이 Codex 로 작업한다. 커밋·푸시가 필요한 변경은
  **왜 넣는지 먼저 사용자에게 설명하고** 진행한다(2026-08-14 사용자 지시).
- 남의 작업물을 고칠 때는 커밋 이력으로 작성자를 먼저 확인한다. 2026-07-31 갤러리 조사 문서
  (`results/국립대편입갤러리_…md`)를 공개-전-점검 기준으로 고치려다 **철회**했다 — 작성자가
  협업자이고, 적힌 것은 공개 게시글 수동 열람이라 약관 위반 근거가 없었다.
- `data/` 는 gitignore 라 **클론한 사람은 원본 대조를 못 돌린다.** 대조 건수는 커밋 메시지에 남긴다.
- `results/` ↔ `web/src/data/` 는 손으로 고치지 않는다. 고칠 곳은 `tools/apply_fixes.py` 다.
